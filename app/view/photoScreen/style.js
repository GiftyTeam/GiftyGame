import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: '1%',
  },
  timerContainer: {
    top: 12,
    width: '100%',
  },
});

export default styles;
